{
  "name": "logexus-daemon",
  "version": "0.1.0",
  "type": "module",
  "description": "Logexus AI Browser — WebSocket Daemon (message router + auth gateway)",
  "main": "server.js",
  "bin": { "logexus-daemon": "./server.js" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
